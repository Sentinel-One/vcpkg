diff --git a/Release/src/http/client/http_client_winhttp.cpp b/Release/src/http/client/http_client_winhttp.cpp
index 3f09f944..bb089392 100644
--- a/Release/src/http/client/http_client_winhttp.cpp
+++ b/Release/src/http/client/http_client_winhttp.cpp
@@ -966,6 +966,18 @@ protected:
             return;
         }
 
+        auto ms = client_config().timeout<std::chrono::milliseconds>();
+        if (m_prev_timeout.exchange(ms) != ms)
+        {
+            int milliseconds = std::max<int>(static_cast<int>(ms.count()), 1);
+            if (!WinHttpSetTimeouts(m_hSession, milliseconds, milliseconds, milliseconds, milliseconds))
+            {
+                auto errorCode = GetLastError();
+                request->report_error(errorCode, build_error_msg(errorCode, "Error setting new timeout"));
+                return;
+            }
+        }
+
         http_request& msg = request->m_request;
         http_headers& headers = msg.headers();
         std::shared_ptr<winhttp_request_context> winhttp_context =
@@ -2475,6 +2487,7 @@ private:
     }
 
     std::atomic<bool> m_opened;
+    std::atomic<std::chrono::microseconds> m_prev_timeout;
 
     // WinHTTP session and connection
     HINTERNET m_hSession;
